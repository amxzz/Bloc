import React from "react";
import ManagerOverviewSection from "@/components/manager/overview/ManagerOverviewSection";

export const metadata = {
  title: "Manager Operations | Bloc. Gelato",
};

export default function ManagerOverviewPage() {
  return <ManagerOverviewSection />;
}
